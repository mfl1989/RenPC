server:
  port: 8080

spring:
  application:
    name: recycle-backend
  jackson:
    time-zone: Asia/Tokyo
  # DB 接続に失敗すると Flyway が動かずアプリ全体が起動しない（/api も利用不可）。
  # 既定のユーザー recycle / DB recycle はリポジトリ直下の docker compose で作成できる。
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/recycle}
    username: ${DATABASE_USERNAME:recycle}
    password: ${DATABASE_PASSWORD:recycle}
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Tokyo
  mail:
    host: ${MAIL_HOST:}
    port: ${MAIL_PORT:587}
    protocol: ${MAIL_PROTOCOL:smtp}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: ${MAIL_SMTP_AUTH:true}
          connectiontimeout: ${MAIL_SMTP_CONNECTION_TIMEOUT_MS:5000}
          timeout: ${MAIL_SMTP_TIMEOUT_MS:10000}
          writetimeout: ${MAIL_SMTP_WRITE_TIMEOUT_MS:10000}
          ssl:
            enable: ${MAIL_SMTP_SSL_ENABLE:false}
          starttls:
            enable: ${MAIL_SMTP_STARTTLS_ENABLE:true}
            required: ${MAIL_SMTP_STARTTLS_REQUIRED:false}

# JWT 設定（MVP: HS256）
jwt:
  # ここでは「十分に長い秘密文字列」を raw 文字列として扱います（Base64 ではない前提）。
  secret: ${JWT_SECRET:change-me-change-me-change-me-change-me-change-me-change-me}
  # 有効期限（ミリ秒）。86400000 = 24時間
  expiration: ${JWT_EXPIRATION_MS:86400000}

# MVP: 管理者資格情報（開発用の一時的設定）
admin:
  username: ${ADMIN_USERNAME:admin}
  password: ${ADMIN_PASSWORD:admin}

notification:
  mail:
    enabled: ${NOTIFICATION_MAIL_ENABLED:false}
    from: ${NOTIFICATION_MAIL_FROM:}
    from-name: ${NOTIFICATION_MAIL_FROM_NAME:}
    reply-to: ${NOTIFICATION_MAIL_REPLY_TO:}
    contact-admin-to: ${CONTACT_INQUIRY_ADMIN_TO:}
    company-name: ${NOTIFICATION_COMPANY_NAME:RenPC}
    support-desk-name: ${NOTIFICATION_SUPPORT_DESK_NAME:RenPCカスタマーサポート}
    support-hours: ${NOTIFICATION_SUPPORT_HOURS:平日 9:00〜18:00（土日祝日・年末年始を除く）}
    public-base-url: ${PUBLIC_BASE_URL:http://localhost:5173}
